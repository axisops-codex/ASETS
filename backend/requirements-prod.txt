# Production runtime deps (Cloud Run).
# Deliberately excludes `emergentintegrations`, which lives on the Emergent
# platform's private index and is only used when LLM_PROVIDER=emergent.
fastapi==0.110.1
uvicorn[standard]==0.25.0
python-dotenv>=1.0.1
asyncpg>=0.30.0
pydantic>=2.6.4
email-validator>=2.2.0
pyjwt>=2.10.1
bcrypt==4.1.3
cryptography>=42.0.8
python-multipart>=0.0.9
requests>=2.31.0
httpx>=0.27.0
tzdata>=2024.2
anthropic>=1.0.0
